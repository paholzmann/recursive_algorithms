

def factorial(n):
    """
    Calculates the factorial from n recursiv
    Args:
        n (int): A non negative integer
    Returns:
        int: Faculty of n
    Raises:
        ValueError: If n is negative
    """
    if n < 0:
        raise ValueError("Number cannot be negative")
    if n == 0:
        return 1
    return n * factorial(n - 1)

print(factorial(5))
